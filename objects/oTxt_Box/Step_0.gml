srDialogues();
controls();
//Write Effect
if (indice < string_length(page))
{
indice += text_spd
//Sound Effect
if (indice == round(indice))
{audio_play_sound(text_sound,1,false)}
}
//Change Page
if key_B && indice >= string_length(page)
{
if page_nb != page_end
{page_nb += 1;}
indice = 0
}
//Page Numbers
srDialoguePages();